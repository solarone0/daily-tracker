/**
 * Daily Tracker Configuration
 */

const CONFIG = {
    // GitHub API Configuration
    GITHUB: {
        OWNER: 'solarone0',
        REPO: 'daily-tracker',
        FILE_PATH: 'data/index.json',
        BRANCH: 'main'
    },

    // Local storage key for offline data and token
    LOCAL_STORAGE_KEY: 'daily-tracker-data',
    GITHUB_TOKEN_KEY: 'daily-tracker-github-token'
};
